# Colab T4 training stack — keep this file minimal.
#
# Do NOT pin torch or xformers here.
# Colab ships CUDA-matched torch; keep the rest aligned for Unsloth + TRL GRPO.
#
# Local/dev reference only. Colab installs inline pins in notebooks/Colab_T4_Training.ipynb.
# Do not use this file in Colab with a single `pip install -r` — install step-by-step in the notebook.

datasets>=3.4.1,<4.4.0,!=4.0.*,!=4.1.0
requests>=2.32.0

unsloth==2026.4.6

# Keep TRL in the Unsloth-compatible range for Colab T4.
trl>=0.23.0,<=0.24.0,!=0.19.0
mergekit
llm-blender
transformers==4.56.2

# Do not add torchcodec / torchao / sentence-transformers here. They are optional
# for this text-only run and frequently break imports on Colab's CUDA/PyTorch stack.
