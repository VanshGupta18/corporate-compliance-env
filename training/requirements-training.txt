# Colab T4 training stack — keep this file minimal.
#
# Do NOT pin torch, xformers, accelerate, transformers, or peft here.
# Colab ships CUDA-matched torch; Unsloth resolves the HF training stack.
#
# Install order (see notebooks/Colab_T4_Training.ipynb):
#   1. pip install -r requirements.txt
#   2. pip install -r training/requirements-training.txt
#   3. pip uninstall -y torchcodec torchao sentence-transformers

datasets>=3.4.1,<4.4.0
requests>=2.32.0

# Pulls compatible transformers / peft / accelerate / bitsandbytes / trl (<=0.24)
unsloth==2026.4.6

# Pin TRL for GRPO rollout_func + trl.experimental.openenv
trl==0.24.0

# Do not add torchcodec / torchao / sentence-transformers here. They are optional
# for this text-only run and frequently break imports on Colab's CUDA/PyTorch stack.
